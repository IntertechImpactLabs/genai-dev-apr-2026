{
  "name": "D1: Tool Use Under the Hood",
  "build": {
    "dockerfile": "../../.devcontainer-base/Dockerfile",
    "context": "../../.devcontainer-base"
  },
  "workspaceFolder": "/workspaces/07_skills",
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspaces/07_skills,type=bind,consistency=cached",
  "customizations": {
    "vscode": {
      "extensions": [
        "humao.rest-client",
        "dbaeumer.vscode-eslint",
        "Anthropic.claude-code"
      ]
    }
  },
  "mounts": [],
  "runArgs": [
    "--hostname",
    "demo-07-skills",
    "--env-file",
    "${localWorkspaceFolder}/../.env"
  ]
}