{
  "name": "D1: Tool Use Under the Hood",
  "build": {
    "dockerfile": "../../.devcontainer-base/Dockerfile",
    "context": "../../.devcontainer-base"
  },
  "workspaceFolder": "/workspaces/05_refactoring",
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspaces/05_refactoring,type=bind,consistency=cached",
  "customizations": {
    "vscode": {
      "extensions": [
        "humao.rest-client",
        "dbaeumer.vscode-eslint"
      ]
    }
  },
  "mounts": [],
  "runArgs": [
    "--hostname",
    "demo-05-refactoring",
    "--env-file",
    "${localWorkspaceFolder}/../.env"
  ]
}