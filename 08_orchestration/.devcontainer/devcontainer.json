{
  "name": "D1: Tool Use Under the Hood",
  "build": {
    "dockerfile": "../../.devcontainer-base/Dockerfile",
    "context": "../../.devcontainer-base"
  },
  "workspaceFolder": "/workspaces/08_orchestration",
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspaces/08_orchestration,type=bind,consistency=cached",
  "customizations": {
    "vscode": {
      "extensions": [
        "humao.rest-client",
        "dbaeumer.vscode-eslint",
        "Anthropic.claude-code"
      ]
    }
  },
  "mounts": [],
  "runArgs": [
    "--hostname",
    "demo-08-orchestration",
    "--env-file",
    "${localWorkspaceFolder}/../.env"
  ]
}