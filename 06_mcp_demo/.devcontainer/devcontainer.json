{
  "name": "D1: Tool Use Under the Hood",
  "build": {
    "dockerfile": "../../.devcontainer-base/Dockerfile",
    "context": "../../.devcontainer-base"
  },
  "workspaceFolder": "/workspaces/06_mcp_demo",
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspaces/06_mcp_demo,type=bind,consistency=cached",
  "customizations": {
    "vscode": {
      "extensions": [
        "humao.rest-client",
        "dbaeumer.vscode-eslint",
        "Anthropic.claude-code"
      ]
    }
  },
  "mounts": [],
  "runArgs": [
    "--hostname",
    "demo-06-mcp-demo",
    "--env-file",
    "${localWorkspaceFolder}/../.env"
  ]
}